/**
 * Kim Cluster 4 — EN Markers
 * HOOP-K01, SCHAAM-K01, ROUW-K01, ISOL-K01
 */

// ─── HOOP-K01 ─────────────────────────────────────────────────────────────────

export const EN_HOOP_MARKERS: RegExp[] = [
  /\bwhen is enough enough\b/i,
  /\bi don'?t know if (?:this|it) (?:still )?makes? sense\b/i,
  /\bi(?:'m| am) losing (?:all )?hope\b/i,
  /\bi have no hope left\b/i,
  /\bhow long (?:do i|must i|should i) (?:still )?(?:keep|hold) (?:this|on)\b/i,
  /\bi can'?t (?:do this|take this) anymore\b/i,
  /\bi(?:'m| am) tired of hoping\b/i,
  /\bi don'?t believe (?:in (?:it|this) )?anymore\b/i,
  /\bit(?:'s| is) (?:no longer|not) (?:worth it|meaningful)\b/i,
  /\bi(?:'m| am) giving up\b/i,
  /\bi don'?t know if (?:he|she) will ever change\b/i,
  /\bmaybe i should (?:stop|leave|give up)\b/i,
  /\bwhat if (?:it|he|she) never gets better\b/i,
  /\bi(?:'m| am) exhausted from waiting\b/i,
];

// ─── SCHAAM-K01 ───────────────────────────────────────────────────────────────

export const EN_SCHAAM_MARKERS: RegExp[] = [
  /\bi(?:'m| am) ashamed of (?:his|her) addiction\b/i,
  /\bi (?:don'?t|do not) dare (?:to )?tell (?:my )?(?:family|friends|anyone)\b/i,
  /\bi hide it from everyone\b/i,
  /\bi lie to (?:my )?(?:family|friends)\b/i,
  /\bi make excuses for (?:him|her)\b/i,
  /\bi (?:don'?t|do not) want (?:people|anyone) to know\b/i,
  /\bi(?:'m| am) afraid they will judge (?:me|him|her)\b/i,
  /\bi withdraw because i (?:feel|am) ashamed\b/i,
  /\bi (?:don'?t|do not) go anywhere because i (?:feel|am) ashamed\b/i,
  /\bwhat will people think\b/i,
  /\bi feel responsible for (?:his|her) behavio(?:u)?r\b/i,
];

// ─── ROUW-K01 ─────────────────────────────────────────────────────────────────

export const EN_ROUW_MARKERS: RegExp[] = [
  /\bi miss who (?:he|she) used to be\b/i,
  /\bi grieve (?:who )?(?:he|she) was\b/i,
  /\bit feels like i (?:lost|have lost) (?:him|her) (?:while|even though) (?:he|she)(?:'s| is) still alive\b/i,
  /\bi miss (?:our )?(?:old )?relationship\b/i,
  /\bi miss how (?:it|things) used to be\b/i,
  /\bi grieve the future (?:we were|we're) (?:supposed|going) to have\b/i,
  /\bi imagined a different life\b/i,
  /\bi (?:lost|have lost) someone who is still (?:here|alive)\b/i,
  /\bthe person (?:from )?before is gone\b/i,
  /\baddiction (?:has )?changed (?:our|the) relationship\b/i,
  /\bi grieve us\b/i,
  /\bi miss (?:my partner|the safety)\b/i,
];

// ─── ISOL-K01 ─────────────────────────────────────────────────────────────────

export const EN_ISOL_MARKERS: RegExp[] = [
  /\bi (?:don'?t|do not) see anyone anymore\b/i,
  /\bi (?:don'?t|do not) talk to (?:my )?friends anymore\b/i,
  /\bi have no social life (?:anymore|left)\b/i,
  /\bi (?:became|have become|am) isolated\b/i,
  /\bi(?:'m| am) completely alone with this\b/i,
  /\bi have no energy (?:left )?for (?:people|anyone)\b/i,
  /\bi cancel everything\b/i,
  /\bi (?:don'?t|do not) go anywhere anymore\b/i,
  /\bcaregiving consumes everything\b/i,
  /\b(?:his|her) addiction consumes my life\b/i,
  /\bi have no time for myself\b/i,
  /\bi (?:lost|have lost) my (?:own )?contacts\b/i,
  /\bi (?:don'?t|do not) know how to let people in\b/i,
  /\bi (?:don'?t|do not) dare (?:to )?see anyone\b/i,
  /\bi hide from everyone\b/i,
  /\bi have no ?one to talk to (?:about this)?\b/i,
];
